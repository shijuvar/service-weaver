[serviceweaver]
binary = "./hello"

[single]
listeners.hello = {address = "localhost:8082"}

[multi]
listeners.hello = {address = "localhost:8082"}

[gke]
regions = ["us-west1"]
listeners.hello = {public_hostname = "hello.example.com"}